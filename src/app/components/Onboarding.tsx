// src/app/components/Onboarding.tsx
'use client'; // Required if this is a client-side component in Next.js 13+ App Router

import React from 'react';

const OnboardingForm = () => {
  return (
    <div>
      <h1>Welcome to Onboarding</h1>
      {/* Add your onboarding form fields here */}
    </div>
  );
};

export default OnboardingForm; // ✅ default export required